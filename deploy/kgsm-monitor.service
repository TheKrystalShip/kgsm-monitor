[Unit]
Description=KGSM Metrics Monitor
Documentation=https://github.com/TheKrystalShip
After=network.target

[Service]
Type=simple
# Runs as the KGSM user, NOT root. The monitor only READS: cgroup v2 counters (the
# per-instance cgroups under kgsm.slice are world-readable; kgsm-watchdog — the peer —
# OWNS and creates them), this user's /proc, and /sys. It also spawns kgsm.sh, which MUST
# run as the KGSM user to enumerate that user's instances. uid/gid 1000 = the KGSM user,
# matching kgsm-watchdog's KGSM_WATCHDOG_UID/GID. Adjust both lines for a different host.
User=kgsm
Group=kgsm

# Native-AOT single-file binary (publish: dotnet publish src/Monitor -c Release -r linux-x64).
ExecStart=/opt/kgsm-monitor/kgsm-monitor

# The scrape socket the API reads. Lives inside the RuntimeDirectory (below), created by
# systemd owned by this user, so a co-located API running as the same user can read it.
Environment=Monitor__SocketPath=/run/kgsm-monitor/metrics.sock
Environment=Monitor__SocketMode=660

# Per-server sampling ON: point at /usr/local/bin/kgsm — the canonical kgsm symlink every
# consumer shares (kgsm-watchdog points at the same path). Repoint that symlink to move the
# whole host at once, no unit edits. Unset this to run host-only. ProtectHome is deliberately
# NOT set — kgsm's instances/config live under /home and the daemon runs as that home's user.
Environment=Monitor__KgsmPath=/usr/local/bin/kgsm

# The engine's append-only event journal, which the monitor TAILS for instance_* lifecycle
# events — so a started/stopped server is reflected sub-second instead of after
# Monitor__ServerResyncMs, and a monitor that was down catches up from its stored position
# rather than losing what it missed. The periodic resync remains the watch-list's source of
# truth. Nothing to configure on the engine side and nothing reserved here: the engine is the
# sole writer, and any number of consumers read the same files. Readable because the daemon
# runs as the KGSM user that owns the journal (ProtectSystem=strict is deliberately unset —
# see the hardening note below).
Environment=Monitor__KgsmJournalDir=/var/lib/kgsm/events

# Per-server disk footprint (diskBytes): recompute cadence for the working-dir size walk.
# Default 60s; lower it for snappier footprint updates during testing.
# Environment=Monitor__DiskUsageMs=60000

# Host-specific overrides — optional, wins over the above and over the settings file.
# Every key here must exist in kgsm-monitor.settings.json, which declares the daemon's whole
# configurable surface; a variable naming an undeclared key binds to nothing and sets nothing.
EnvironmentFile=-/etc/kgsm-monitor/kgsm-monitor.env

# systemd creates /run/kgsm-monitor owned by this user (mode 0750); the 0660 socket lives in it.
RuntimeDirectory=kgsm-monitor
RuntimeDirectoryMode=0750

# systemd creates /var/lib/kgsm-monitor owned by this user (persistent across reboots) for the
# metrics-history SQLite store (Monitor__HistoryDbPath defaults inside it). The runtime dir above is
# tmpfs — unsuitable for a DB that must survive a restart.
StateDirectory=kgsm-monitor
StateDirectoryMode=0750

Restart=always
RestartSec=2

# Hardening kept minimal/compatible: the daemon reads /proc, /sys, /sys/fs/cgroup and /home
# (kgsm.sh + instance config) and writes only its RuntimeDirectory socket. ProtectHome/
# ProtectSystem=strict are intentionally omitted — they'd hide or freeze the /home paths
# kgsm.sh needs. AF_UNIX is the only socket family it uses (bpf() is not socket/AF-gated).
NoNewPrivileges=true
PrivateTmp=true
RestrictAddressFamilies=AF_UNIX

# Per-server network (Phase 1): the monitor reads the pinned eBPF net-meter map via bpf(),
# which this host gates behind cap_bpf (kernel.unprivileged_bpf_disabled=2). With
# NoNewPrivileges=true a setcap'd file capability would be IGNORED, so we grant the cap the
# systemd-native way: AmbientCapabilities raises CAP_BPF into the (User=kgsm) service and
# survives the uid switch, and it's compatible with NoNewPrivileges. The bounding set is
# narrowed to just CAP_BPF (read-flavoured: cannot modify networking, isn't root). If the
# net-meter isn't set up (deploy/kgsm-net-meter.service not run), the monitor simply reports
# RxBps/TxBps as null — this cap is harmless when unused. Drop both lines to run without it.
AmbientCapabilities=CAP_BPF
CapabilityBoundingSet=CAP_BPF

[Install]
WantedBy=multi-user.target
