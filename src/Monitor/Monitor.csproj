<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>

    <!-- Native AOT: small footprint, no JIT, instant start — the daemon must not
         steal resources from the game servers it watches. -->
    <PublishAot>true</PublishAot>
    <InvariantGlobalization>true</InvariantGlobalization>
    <IsAotCompatible>true</IsAotCompatible>

    <RootNamespace>TheKrystalShip.KGSM.Monitor</RootNamespace>
    <AssemblyName>kgsm-monitor</AssemblyName>
    <Version>1.10.0</Version>

    <!-- Required by the source-generated [LibraryImport] sysconf(_SC_CLK_TCK) p/invoke in
         ProcTreeSampler (Slice 3). The generated marshalling stub uses unsafe; no hand-written
         unsafe code in the project. -->
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>

    <!-- The <panel> doc tags on MonitorSettings are where the Control Panel's field descriptions come
         from; the compiler's XML output is how the descriptor generator reads them. CS1591 stays off:
         the daemon's own internals are not a documented public API, and turning it on would demand a
         doc comment on every type to get the twenty-odd that matter. -->
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <NoWarn>$(NoWarn);CS1591</NoWarn>
  </PropertyGroup>

  <ItemGroup>
    <!-- The leaf config descriptor attributes, compiled in as source rather than referenced as a
         package. There is no assembly to load, trim or reason about at runtime — the leaf never reads
         them; tools/LeafDescriptorGen reads them back out of this assembly's metadata. That is what
         keeps a described leaf byte-for-byte as AOT-clean as an undescribed one. -->
    <Compile Include="..\LeafConfig\*.cs" LinkBase="LeafConfig" />
  </ItemGroup>

  <ItemGroup>
    <!-- Expose internal pure parse/rate helpers + Build() to test and benchmark projects. -->
    <InternalsVisibleTo Include="Monitor.Tests" />
    <InternalsVisibleTo Include="Monitor.Benchmarks" />
  </ItemGroup>

  <ItemGroup>
    <!-- The daemon's settings file, renamed from appsettings.json so it can't collide with a sibling
         ecosystem service's config if they ever share a directory. The Web SDK only auto-copies the
         literal "appsettings.json", so the renamed file needs an explicit item to reach bin/ + publish/.
         Program.cs loads it from AppContext.BaseDirectory; deploy installs it beside the binary. -->
    <Content Update="kgsm-monitor.settings.json" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

  <ItemGroup>
    <!-- The shared GET /metrics wire contract (Snapshot graph + source-gen JSON). The
         monitor owns the shape and references it by project so a contract change is a
         compile break here; the same source is packed and consumed by kgsm-api. -->
    <ProjectReference Include="..\Monitor.Contracts\Monitor.Contracts.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- The single KGSM chokepoint (instance resync + event watch + resolution metadata) for
         per-server cgroup metrics AND (1.36.0+) engine event-history persistence. Consumed as the
         published NuGet package; the net9 lib is IsAotCompatible, so consuming it from this net10 AOT
         app is a verified pattern. Instance.ports is the structured array
         ([{start,end,protocol}], the canonical port format); Instance.CgroupPath supplies native
         cgroup addressing for the resolver. 1.36.0 adds IEventService.RegisterRawHandler (every
         deserialized event envelope, known or unknown type — src/Monitor/History/EventPersistService.cs)
         and Events.AuditId.ForEvent (the deterministic evt_<hash> id shared with kgsm-api, so both
         sides compute the same id with zero coordination). -->
    <PackageReference Include="TheKrystalShip.KGSM.Lib" Version="2.0.0" />
  </ItemGroup>

  <ItemGroup>
    <!-- Raw ADO SQLite for the metrics-history store (the monitor is the single source of truth for
         history). Microsoft.Data.Sqlite (NOT EF Core — EF is not AOT-safe) bundles the e_sqlite3 native
         lib via SQLitePCLRaw.bundle_e_sqlite3, which the AOT publish emits alongside the binary. -->
    <PackageReference Include="Microsoft.Data.Sqlite" Version="10.0.5" />
  </ItemGroup>

  <!-- Scoped NuGet-audit suppression (NOT a blanket NuGetAudit=off): GHSA-2m69-gcr7-jv3q is an
       advisory on the native SQLite lib pulled TRANSITIVELY by Microsoft.Data.Sqlite
       (SQLitePCLRaw.lib.e_sqlite3 2.1.11) — the latest published build of that native lib, no fixed
       version to move to yet. Exposure is minimal: SQLite stores only this host's local metrics history
       on a trusted host (no untrusted SQL, no network DB). DELETE this when a patched bundle ships. -->
  <ItemGroup>
    <NuGetAuditSuppress Include="https://github.com/advisories/GHSA-2m69-gcr7-jv3q" />
  </ItemGroup>

  <!-- Regenerate the leaf config descriptor from this assembly's own metadata, every build. There is
       no step to remember and no second place to edit: a knob added to MonitorSettings appears on the
       Control Panel's configuration page as soon as the daemon compiles, and a knob declared in the
       settings file that nothing describes fails the build naming the key.

       It runs against the IL assembly, which exists under both `dotnet build` and the AOT publish, so
       the descriptor is refreshed on the same command that produces the binary it describes. -->
  <Target Name="GenerateLeafDescriptor" AfterTargets="Build" Condition="'$(DesignTimeBuild)' != 'true'">
    <PropertyGroup>
      <_LeafDescriptorGen>$(MSBuildProjectDirectory)/../../tools/LeafDescriptorGen/LeafDescriptorGen.csproj</_LeafDescriptorGen>
      <_LeafDescriptor>$(MSBuildProjectDirectory)/../../deploy/kgsm-monitor.leaf.json</_LeafDescriptor>
    </PropertyGroup>

    <!-- The tool is an ordinary portable build and must stay one. Without RemoveProperties, an AOT
         publish passes its own RuntimeIdentifier/PublishAot down and the tool fails to restore for a
         RID it never targets. -->
    <MSBuild Projects="$(_LeafDescriptorGen)" Targets="Restore;Build" Properties="Configuration=Release"
             RemoveProperties="RuntimeIdentifier;SelfContained;PublishAot;PublishTrimmed;PublishSingleFile;TargetFramework">
      <Output TaskParameter="TargetOutputs" ItemName="_LeafDescriptorGenBinary" />
    </MSBuild>

    <Exec Command="dotnet &quot;@(_LeafDescriptorGenBinary)&quot; --assembly &quot;@(IntermediateAssembly->'%(FullPath)')&quot; --settings &quot;$(MSBuildProjectDirectory)/kgsm-monitor.settings.json&quot; --out &quot;$(_LeafDescriptor)&quot;" />
  </Target>

  <Target Name="SetSourceRevisionId" BeforeTargets="GetAssemblyVersion;GenerateAssemblyInfo;CoreCompile"
          Condition="'$(SourceRevisionId)' == ''">
    <Exec Command="git -C &quot;$(MSBuildProjectDirectory)&quot; rev-parse --short=12 HEAD"
          ConsoleToMSBuild="true" ContinueOnError="true" StandardErrorImportance="low" StandardOutputImportance="low">
      <Output TaskParameter="ConsoleOutput" PropertyName="_GitShortSha" />
      <Output TaskParameter="ExitCode" PropertyName="_GitExitCode" />
    </Exec>
    <PropertyGroup Condition="'$(_GitExitCode)' == '0' and '$(_GitShortSha)' != ''">
      <SourceRevisionId>$(_GitShortSha)</SourceRevisionId>
    </PropertyGroup>
  </Target>

</Project>
